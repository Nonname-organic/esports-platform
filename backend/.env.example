# App
ENVIRONMENT=demo
DEBUG=false

# Security (32文字以上のランダム文字列)
SECRET_KEY=change-me-to-a-random-256bit-secret-key-here

# Database (RDS エンドポイント)
DB_HOST=your-rds-endpoint.ap-northeast-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=esports_db
DB_USER=esports_user
DB_PASSWORD=your-secure-password

# Redis (Docker Compose内はコンテナ名)
REDIS_HOST=redis
REDIS_PORT=6379

# AWS
AWS_REGION=ap-northeast-1
S3_BUCKET_NAME=esports-assets-your-account-id
SQS_MATCH_QUEUE_URL=https://sqs.ap-northeast-1.amazonaws.com/123456789/match-queue
SQS_NOTIFICATION_QUEUE_URL=https://sqs.ap-northeast-1.amazonaws.com/123456789/notification-queue

# Discord (任意)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your-webhook-url

# CORS (フロントエンドのURL)
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com

# ── 運用（scripts/backup-db.sh, healthcheck.sh が読む） ──────────────────────
# DBバックアップのアップロード先。未設定ならローカル保存のみ。
# Cloudflare R2 を使う場合は S3_ENDPOINT_URL も設定する
BACKUP_S3_BUCKET=
BACKUP_KEEP_DAYS=14
# 例: https://<account_id>.r2.cloudflarestorage.com
S3_ENDPOINT_URL=
# 稼働点検の通知先（Discord Webhook）。未設定ならログ出力のみ
HEALTHCHECK_WEBHOOK_URL=

# APIドキュメント(/docs)を公開するか。インターネット公開時は false のまま
EXPOSE_API_DOCS=false

# Sentry (任意)
SENTRY_DSN=
